[[requirements]]
id = "KS-9-001"
section = "9 Operator overloading — regular and conventional calls"
printed_page = 200
statement = "Operator functions use the operator modifier, remain callable normally, and also participate in syntax conventions."
classification = "exact"
capabilities = ["syntax diagnostics", "semantic tokens"]
status = "active"
tests = ["ks_9_001_operator_functions_support_regular_calls_and_operator_conventions"]
duplicates = []
fixture = "A same-file plus operator is used through both plus() and +."
sample_evidence = "Android domain values expose conventional and explicit operator calls."
oracle = "kotlin-spec.pdf chapter 9 printed pages 199-200; clean CST."
fallback_oracle = "Not used; declaration and calls are local."

[[requirements]]
id = "KS-9-002"
section = "9 Operator overloading — declaration locations"
printed_page = 200
statement = "Suitable operator functions may be members or extensions and may be suspending or non-suspending."
classification = "exact"
capabilities = ["syntax diagnostics", "semantic tokens"]
status = "active"
tests = ["ks_9_002_operator_functions_may_be_members_extensions_or_suspending"]
duplicates = []
fixture = "Member and extension unary/binary operators include suspend and ordinary forms."
sample_evidence = "Android domain and coroutine APIs define operators in multiple declaration scopes."
oracle = "kotlin-spec.pdf chapter 9 printed page 200; clean CST."
fallback_oracle = "Not used; all four declaration forms are explicit."

[[requirements]]
id = "KS-9-003"
section = "9 Operator overloading — operator-only expansion calls"
printed_page = 199
statement = "Calls produced by convention expansion may use only functions declared as operator."
classification = "exact"
capabilities = ["syntax diagnostics", "definition"]
status = "ignored"
tests = ["ks_9_003_operator_convention_requires_the_operator_modifier"]
duplicates = []
fixture = "Operator plus is valid while an otherwise identical ordinary plus used through + is invalid."
sample_evidence = "Android types may have ordinary methods whose names coincide with operator conventions."
oracle = "kotlin-spec.pdf chapter 9 printed pages 199-200; explicit modifiers."
fallback_oracle = "Not used; candidates are same-file and non-generic."
ignore_reason = "Observed red after the operator positive parsed: + with a non-operator plus also has a clean CST and no diagnostic."
expected_behavior = "The + expression backed only by ordinary plus must be diagnosed."

[[requirements]]
id = "KS-9-004"
section = "9 Operator overloading — hygienic expansion"
printed_page = 199
statement = "Identifiers introduced by a convention expansion are inaccessible outside it and cannot clash with program declarations."
classification = "compiler-only"
capabilities = ["references", "definition"]
status = "not-applicable"
tests = []
duplicates = []
fixture = "User declarations resembling expansion temporaries require compiler-generated-name isolation."
sample_evidence = "Android code may use temporary-like local names around complex operators."
oracle = "kotlin-spec.pdf chapter 9 printed page 199."
fallback_oracle = "Not used; hygiene rule is explicit."
exclusion_rationale = "Verification requires observing compiler-only expansion identifiers and lexical isolation."

[[requirements]]
id = "KS-9-005"
section = "9 Operator overloading — call-by-need operands"
printed_page = 199
statement = "Expressions captured by an expansion are evaluated once at their first expansion use."
classification = "compiler-only"
capabilities = ["hover", "definition"]
status = "not-applicable"
tests = []
duplicates = []
fixture = "Side-effecting indexed receivers expose whether repeated expansion text is evaluated once."
sample_evidence = "Android operator chains may contain expensive or stateful receivers."
oracle = "kotlin-spec.pdf chapter 9 printed pages 199-201."
fallback_oracle = "Not used; call-by-need is explicit."
exclusion_rationale = "Verification requires runtime evaluation counts after recursive operator expansion."

[[requirements]]
id = "KS-9-006"
section = "9 Operator overloading — recursive priority expansion"
printed_page = 201
statement = "Conventions expand recursively in operator-priority order, and newly produced syntax may itself expand."
classification = "compiler-only"
capabilities = ["definition", "hover"]
status = "not-applicable"
tests = []
duplicates = []
fixture = "Nested C[0][0]++ requires increment, assignment, indexing, get, set, and inc expansions."
sample_evidence = "Android collection wrappers may combine indexed access and mutation operators."
oracle = "kotlin-spec.pdf chapter 9 printed pages 200-201; explicit worked expansion."
fallback_oracle = "Not used; expansion order is explicit."
exclusion_rationale = "Verification requires compiler expansion trees, overload resolution at each stage, mutation, and runtime evaluation."

[[requirements]]
id = "KS-9-007"
section = "9 Operator overloading — platform criteria"
printed_page = 200
statement = "Platforms may impose additional criteria for suitability as an operator candidate."
classification = "compiler-only"
capabilities = ["syntax diagnostics", "definition"]
status = "not-applicable"
tests = []
duplicates = []
fixture = "No fixed common oracle exists for optional platform-specific suitability rules."
sample_evidence = "Android/JVM interop may add operator candidate constraints."
oracle = "kotlin-spec.pdf chapter 9 printed page 200."
fallback_oracle = "Not used; variability is explicit."
exclusion_rationale = "Criteria depend on target platform, compiler implementation, and interop model."

[[requirements]]
id = "KS-9.1-001"
section = "9.1 Destructuring declarations — supported contexts"
printed_page = 201
statement = "Destructuring convention is available for local properties, lambda parameters, and for-loop iteration variables."
classification = "exact"
capabilities = ["syntax diagnostics", "semantic tokens"]
status = "active"
tests = ["ks_9_1_001_destructuring_convention_applies_to_locals_lambdas_and_for_loops"]
duplicates = ["ks_declarations_0304_destructuring_introduces_one_local_name_per_entry", "ks_statements_0036_for_loop_accepts_annotated_variable_or_destructuring_declaration", "ks_expressions_0333_lambda_literal_accepts_destructuring_parameter"]
fixture = "One data class is destructured in all three permitted contexts."
sample_evidence = "Android state and collection transformations destructure values across these contexts."
oracle = "kotlin-spec.pdf 9.1 printed pages 201-202; clean CST."
fallback_oracle = "Not used; contexts are explicit."

[[requirements]]
id = "KS-9.1-002"
section = "9.1 Destructuring declarations — typed and ignored placeholders"
printed_page = 202
statement = "Each destructuring placeholder may have a type, and underscore is a special ignoring placeholder rather than an identifier."
classification = "exact"
capabilities = ["syntax diagnostics", "semantic tokens"]
status = "active"
tests = ["ks_9_1_002_destructuring_placeholders_accept_types_and_ignore_markers"]
duplicates = ["ks_declarations_0306_destructuring_ignore_marker_introduces_no_name", "ks_syntax_0306_lambda_parameter_accepts_variable_with_typed_destructuring"]
fixture = "A typed first entry, underscore middle entry, and typed third entry parse in one local declaration."
sample_evidence = "Android tuple-like results often ignore metadata while typing retained values."
oracle = "kotlin-spec.pdf 9.1 printed pages 201-202; clean CST."
fallback_oracle = "Not used; placeholder syntax is explicit."

[[requirements]]
id = "KS-9.1-003"
section = "9.1 Destructuring declarations — operator components"
printed_page = 202
statement = "Each retained placeholder uses the corresponding zero-argument operator componentK function."
classification = "exact"
capabilities = ["syntax diagnostics", "definition"]
status = "ignored"
tests = ["ks_9_1_003_destructuring_requires_operator_component_functions"]
duplicates = []
fixture = "Operator component1 is valid while an ordinary component1 in the same shape is invalid."
sample_evidence = "Android models and custom tuples expose component functions."
oracle = "kotlin-spec.pdf 9.1 printed pages 201-202; explicit local declarations."
fallback_oracle = "Not used; only modifier differs."
ignore_reason = "Observed red after the operator component positive parsed: destructuring with ordinary component1 also has a clean CST and no diagnostic."
expected_behavior = "The destructuring backed only by non-operator component1 must be diagnosed."

[[requirements]]
id = "KS-9.1-004"
section = "9.1 Destructuring declarations — ignore behavior"
printed_page = 202
statement = "An underscore performs no component call or assignment, but a typed underscore still checks component applicability during inference."
classification = "compiler-only"
capabilities = ["definition", "hover", "references"]
status = "not-applicable"
tests = []
duplicates = []
fixture = "Side-effecting component2 and a typed underscore distinguish no call from applicability checking."
sample_evidence = "Android destructuring may skip expensive or irrelevant components."
oracle = "kotlin-spec.pdf 9.1 printed pages 201-202."
fallback_oracle = "Not used; behavior is explicit."
exclusion_rationale = "Verification requires overload applicability, type inference, and runtime component call counts."

[[requirements]]
id = "KS-9.1-005"
section = "9.1 Destructuring declarations — expansion"
printed_page = 202
statement = "The immediate value is evaluated once and retained placeholders receive componentK results in positional order."
classification = "compiler-only"
capabilities = ["definition", "hover"]
status = "not-applicable"
tests = []
duplicates = []
fixture = "Side-effecting initializer and component methods require runtime call-order observation."
sample_evidence = "Android tuple factories may perform work during creation and component access."
oracle = "kotlin-spec.pdf 9.1 printed pages 201-202; explicit expansions."
fallback_oracle = "Not used; expansion is explicit."
exclusion_rationale = "Verification requires runtime evaluation counts, operator dispatch, and positional assignment values."
